<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguyentrungkhanhb17dccn343">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NguyenTrungKhanhB17DCCN343">
        <activity android:name=".MajorTeahcerActivity"></activity>
        <activity android:name=".AddMajorActivity" />
        <activity android:name=".AddTeacherActivity" />
        <activity android:name=".MajorActivity" />
        <activity android:name=".TeaacherActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>